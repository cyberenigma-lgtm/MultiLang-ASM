# Listado de Países y sus Lenguas Principales (Versión Base)
Optimizado para MultiLang‑ASM v0.7 y futuras expansiones lingüísticas.

Este documento detalla la cobertura regional de MultiLang-ASM, permitiendo a los desarrolladores identificar qué paquetes de idioma deben utilizar según su ubicación o mercado objetivo.

## 🇪🇺 Europa
| País | Lenguas Soportadas (Código) |
| :--- | :--- |
| **España** | Español (`es`), Catalán (`ca`), Gallego (`gl`), Euskera (`eu`) |
| **Francia** | Francés (`fr`) |
| **Alemania** | Alemán (`de`) |
| **Italia** | Italiano (`it`) |
| **Portugal** | Portugués (`pt`) |
| **Reino Unido** | Inglés (`en`) |
| **Irlanda** | Inglés (`en`), Irlandés (`ga`) |
| **Países Bajos** | Neerlandés (`nl`) |
| **Bélgica** | Francés (`fr_be`), Neerlandés (`nl`), Alemán (`de`) |
| **Suecia** | Sueco (`sv`) |
| **Noruega** | Noruego (`no`) |
| **Finlandia** | Finlandés (`fi`), Sueco (`sv`) |
| **Polonia** | Polaco (`pl`) |
| **Rumanía** | Rumano (`ro`) |
| **Ucrania** | Ucraniano (`uk`) |
| **Grecia** | Griego (`el`) |
| **Rusia** | Ruso (`ru`) |

## 🌎 América
| País | Lenguas Soportadas (Código) |
| :--- | :--- |
| **Estados Unidos** | Inglés (`en`), Español (`es`) |
| **Canadá** | Inglés (`en`), Francés (`fr`) |
| **México** | Español (`es_mx`), Náhuatl (`nah`), Maya (`may`) |
| **Argentina** | Español (`es_ar`) |
| **Chile** | Español (`es`) |
| **Colombia** | Español (`es`) |
| **Perú** | Español (`es`), Quechua (`qu`), Aimara (`ay`) |
| **Brasil** | Portugués (`pt`) |
| **Venezuela** | Español (`es`) |
| **Ecuador** | Español (`es`), Quechua (`qu`) |
| **Bolivia** | Español (`es`), Quechua (`qu`), Aimara (`ay`) |

## 🌍 África
| País | Lenguas Soportadas (Código) |
| :--- | :--- |
| **Nigeria** | Inglés (`en`), Hausa (`ha`), Yoruba (`yo`), Igbo (`ig`) |
| **Kenia** | Suajili (`sw`), Inglés (`en`) |
| **Sudáfrica** | Inglés (`en`), Zulú (`zu`), Afrikaans (`af`) |
| **Egipto** | Árabe (`ar`) |
| **Marruecos** | Árabe (`ar`), Bereber (`ber`) |
| **Etiopía** | Amárico (`am`) |
| **Tanzania** | Suajili (`sw`), Inglés (`en`) |
| **Ghana** | Inglés (`en`), Akan (`ak`) |
| **Argelia** | Árabe (`ar`), Bereber (`ber`) |

## 🌏 Asia
| País | Lenguas Soportadas (Código) |
| :--- | :--- |
| **China** | Mandarín (`zh`), Cantonés (`yue`) |
| **India** | Hindi (`hi`), Inglés (`en`), Bengalí (`bn`), Tamil (`ta`), Telugu (`te`) |
| **Japón** | Japonés (`ja`) |
| **Corea del Sur** | Coreano (`ko`) |
| **Indonesia** | Indonesio (`id`), Javanés (`jv`), Sundanés (`su`) |
| **Tailandia** | Tailandés (`th`) |
| **Vietnam** | Vietnamita (`vi`) |
| **Filipinas** | Tagalo (`tl`), Inglés (`en`) |
| **Irán** | Persa (`fa`) |
| **Arabia Saudí** | Árabe (`ar`) |
| **Israel** | Hebreo (`he`), Árabe (`ar`) |

## 🦘 Oceanía
| País | Lenguas Soportadas (Código) |
| :--- | :--- |
| **Australia** | Inglés (`en`) |
| **Nueva Zelanda** | Inglés (`en`), Maorí (`mi`) |
| **P. N. Guinea** | Tok Pisin (`tpi`) |

---
*Nota: Los códigos de idioma corresponden a los archivos en la carpeta `langs/` de MultiLang-ASM.*
